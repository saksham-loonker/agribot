pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = 'AgribotNativeAndroid'

include ':app'
include ':core'
include ':domain'
include ':data'
include ':ml'
include ':camera'
include ':design-system'
include ':feature-scan'

project(':design-system').projectDir = new File('designsystem')
project(':feature-scan').projectDir = new File('featurescan')
