# Agribot Inference Data

Live Pi runs are written here by `43_low_power_5v3a_experiment.py`.

Each run is stored under `runs/<run_id>/` with:

- `metadata.json`
- `summary.json`
- `decisions.jsonl`
- `events.csv`

`latest_run.json` points the platform to the newest run.
